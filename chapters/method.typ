= Methods

#lorem(50)

The methods were developed by @garfinkelDigitalForensicsResearch2010 and @fukamiNewModelForensic2021.

== Method I

#lorem(50)

#figure(
  table(
    columns: (1fr, auto, auto),
    inset: 10pt,
    align: horizon,
    table.header(
      [], [*Volume*], [*Parameters*],
    ),
    image("/images/cylinder.svg", width: 30%),
    $ pi h (D^2 - d^2) / 4 $,
    [
      $h$: height \
      $D$: outer radius \
      $d$: inner radius
    ],
    image("/images/dodecahedron.svg", width: 30%),
    $ sqrt(2) / 12 a^3 $,
    [$a$: edge length],
  ),
  caption: [Table of volumes],
) <table-volumes>

#lorem(50)

@Goddess and @table-volumes are consistent with @garfinkelDigitalForensicsResearch2010.

== Method II

#lorem(50)

#lorem(50)

The first methods was further explained in @app1. The calculations were set out in @app2.

== Summary

#lorem(50)